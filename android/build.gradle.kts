// Raiz do projeto: só declara os plugins, sem aplicá-los aqui.
// Cada módulo (só existe "app" por enquanto) aplica o que precisa.
plugins {
    id("com.android.application") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
