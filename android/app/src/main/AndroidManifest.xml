<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Só para o botão "Testar conexão com a API" no esqueleto. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoleDaFala"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic: a API local em desenvolvimento roda em http,
          não https. Remover (ou restringir por domínio) antes de qualquer
          publicação real — ver docs/2-Arquitetura.md#próximos-passos.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
